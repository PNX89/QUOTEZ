"""Test package.

This file exists so the shared constants in `conftest.py` can be imported by absolute path
(`from tests.conftest import ...`) rather than relying on pytest's sys.path insertion.
"""
